import React from 'react';
import Login from './login'; // Login 컴포넌트가 있는 경로를 정확히 지정하세요.

function App() {
  return (
    <div className="App">
      <Login /> {/* Login 컴포넌트를 렌더링 */}
    </div>
  );
}

export default App;
